---

standalone_applications_folder: "{{ lookup('env', 'HOME') }}/Applications"
standalone_applications: []

# find appropriate version here: https://github.com/marcosnils/bin/releases
bin_base_version: "0.13.1"
cli_apps: []

pipx_apps: []

desktop_entries: []

# should only be used if yt-dlp is one of the
# pipx apps
yt_dlp_conf_dir: "{{ lookup('env', 'HOME') }}/.config/yt-dlp/config"